# 人生日历 · 需求与版本规划 ROADMAP

> 本文档记录已想到的功能点、优先级、复杂度评估与版本路线图，并给出当前迭代版本（v0.4）的设计方案。
> 维护原则：宁缺毋滥，每个候选都要回答「它是否深化了『把一生画成一张表』这一核心隐喻」。
>
> **立项判断框架（2026-08 确立）**：一个 feature 值得做，当且仅当四条同时成立——
> ① **市场缺位**：市场上缺这样的产品或功能；② **用户需要**：有真实诉求（定性反馈验证，见 §6）；
> ③ **调性契合**：不违反 §0 红线与产品气质；④ **规划一致**：符合 §0.5 平台战略的阶段定位。
> 四条是 AND 关系，缺一不做或暂缓。

---

## 0. 不可破的红线（贯穿所有版本）

- **零依赖、零构建、零后端** —— 原生 ES Modules，不引入框架/打包器/服务器
- **数据归用户** —— 一切数据存 `chrome.storage`（sync/local）或导出文件；任何云同步、账号体系、第三方服务一律不做
- **装饰必有主题语义** —— 耐看不花哨，第一万次打开依然安静得体
- **新权限必须可论证** —— 仅当功能无法在现有权限下实现时才申请，并在 release notes 说明用途
- **不做用户行为追踪/分析** —— 因此"验证需求"只能靠 CWS 评论、邮件等定性反馈，见 §6
- **日格只读（美学红线）** —— 日格是「时间流逝」的静态呈现，是设计签名元素；任何需要日格承载输入交互的功能（如 F-12）必须先论证交互冲突，论证不过则保持只读
- **签名纯净** —— 不引入天气/待办/时钟等与「人生表」无关的 widget，不稀释核心隐喻
- **不做游戏化** —— 不引入连续打卡 streak、成就、积分等机制；游戏化与「安静得体」的气质直接冲突

> 以上红线约束的是**浏览器插件**这一载体。平台的长期演进见 §0.5。

---

## 0.5 平台战略：三阶段演进（2026-08 确立）

1. **浏览器插件（当前阶段）**：只做基础功能，保持 §0 全部红线。目标是**低成本完成市场验证**、积累用户量
2. **Web 系统（验证成立后）**：功能做深做强的主战场——账号体系、云同步、重交互能力（每日记录 F-12、多人日历 F-15 等）归属此阶段，**不塞进插件**
3. **App（用户粘性强时）**：Web 系统验证粘性后再启动

**当前阶段纲领：不好高骛远。** 插件侧一切投入以「低成本市场验证」为纲——能快速上线、能拿到定性反馈、不背架构包袱。Web/App 阶段的架构决策届时另行记录，本文档只规划插件侧。

---

## 1. 已交付基线（v0.3.0，已上架 Chrome Web Store）

| 模块 | 内容 |
| --- | --- |
| 人生表 | 5×16 年格 + 当年 12 月逐日格；年/日格等宽对齐；过去灰/未来白/今天红；灰色每日自动推进 |
| 主题 | 5 预制主题（公有领域名画取色）+ 自定义主题编辑器 + 毛玻璃滑杆 + 主题栏快捷切换 |
| 里程碑 | 生日/纪念日/目标日，5 图标，支持每年重复或一次性，年格右上角标记点 |
| 内容 | 每日一句 + 历史上的今天，可独立开关，离线可用 |
| i18n | 简中/繁中/日/韩/英，主页语言按钮一键切换，默认跟随系统 |
| 体验 | 首次引导、跨天自动刷新、时区指定、设置弹窗与独立设置页 |

### 1.1 已知技术债（来自 v0.3 代码 review，未修）

| 编号 | 问题 | 现状 | 危险度 |
| --- | --- | --- | --- |
| TD-01 | `settings-overlay` click 绑定重复 | `bindEvents()` 与 `openSettingsModal()` 各绑一次遮罩点击关闭；因 `closeSettingsModal` 幂等当前无可见 bug，但是冗余代码 + 隐患 | 低（当前）/ 中（若关闭逻辑变非幂等） |
| TD-02 | `IS_DEV` 在定义前被使用 | `init()` 内 line 47 用 `IS_DEV`，`const IS_DEV` 在 line 60 声明；依赖 async 时序碰巧不报错，属脆弱写法 | 低（当前）/ 中（重构时易踩雷） |
| TD-03 | 存储写入无错误处理 | `saveRaw`/`saveBgImage` 不捕获 `chrome.storage` quota 超限，静默失败 | 中（用户数据丢失风险） |
| TD-04 | i18n `t()` 缺键静默 | 缺 key 返回 key 本身，无 dev 告警，漏译不易发现 | 低 |
| TD-05 | 零自动化测试 | 2700 行代码全靠截图目检；F-03 导入校验、A2 农历表、B1 解锁日期计算都是「错了不是丑，是数据错」的功能 | 中（回归风险随功能增长放大） |

---

## 2. 等级定义

### 优先级（4 级）

| 级别 | 含义 | 决策 |
| --- | --- | --- |
| **P0 必做** | 核心体验必需 / 已知 bug 影响数据安全 | 阻塞版本发布，无条件排期 |
| **P1 应做** | 显著提升价值或可靠性 / 现有用户的质量缺陷 | 本版本目标，尽力完成 |
| **P2 可做** | 增量价值，锦上添花 | 视用户反馈与排期择机 |
| **P3 暂缓** | 探索性或受硬约束权衡 | 需先验证需求或解决权衡，不承诺时间 |

### 复杂度（4 级）

| 级别 | 工作量 | 范围特征 |
| --- | --- | --- |
| **S 简单** | 0.5–1 天 | 局部改动，单文件或单函数 |
| **M 中等** | 2–4 天 | 单模块或跨 2–3 文件，可能含新 UI 控件 |
| **L 复杂** | 5–10 天 | 跨多模块，需新数据模型 / 迁移 / 较大 UI |
| **XL 极复杂** | > 10 天 | 架构级或全新子系统，需独立设计阶段 |

> 复杂度评估含 i18n 五语言、测试、截图验证，不是单纯写代码时间。

---

## 3. 候选功能清单

> 编号约定：TD = 技术债；F = 原功能清单；A = 时间观（寿命/农历）；B = 仪式感与内容（胶囊/名言/历史/名人）；C = 数据洞察；D = 平台移植；E = 内容资产扩充；G = 情感深化；H = 增长激活；V = 视图与钻取导航（年/月/周/日分辨率）。

| 编号 | 功能 | 优先级 | 复杂度 | 目标版本 | 说明 |
| --- | --- | --- | --- | --- | --- |
| **—— 技术债 ——** | | | | | |
| TD-03 | 存储写入错误处理 | P0 | S | v0.4 | 见 §1.1；数据安全相关，优先于新功能 |
| TD-01 | 去除 settings-overlay 重复绑定 | P1 | S | v0.4 | 删 `bindEvents` 内那行，保留 `openSettingsModal` 内带守卫的绑定 |
| TD-02 | IS_DEV 前置 + dev 参数精简 | P1 | S | v0.4 | 移到模块顶部消除时序依赖；删除 `?year=`/`?lang=open` 边角参数，保留截图必需参数 |
| TD-04 | i18n 缺键 dev 告警 | P1 | S | v0.4 | `t()` 缺键时 `console.warn`，顺手做掉 |
| TD-05 | 最小单元测试 | P1 | S | v0.6 | `node:test`（Node 内置，零依赖不违反红线）：覆盖 `date.js` 闰年/跨年边界、导入校验；为 A2 农历表与 B1 日期计算打底 |
| **—— 数据可携带（v0.4）——** | | | | | |
| F-02 | 数据导出 | P1 | M | v0.4 | JSON 导出全部设置+里程碑+自定义主题+背景图 |
| F-03 | 数据导入 | P1 | M | v0.4 | JSON 导入并校验，换设备/重装无忧 |
| F-06 | 里程碑 CSV 导出 | P2 | S | v0.4 | 里程碑清单导出 CSV，便于表格软件查看 |
| **—— 可访问性 ——** | | | | | |
| F-05 | 键盘导航年格 | P2 | S | v0.4 | 方向键浏览 + Enter 切月表，补齐 a11y |
| F-11 | 首日体验优化 | P2 | S | v0.5 | 引导卡预览主题、时区检测提示更友好 |
| **—— 观览延展（v0.5）——** | | | | | |
| F-09 | 纪念日倒计时 | P2 | M | v0.5 | 标题区显示距下一个里程碑的天数 |
| F-08 | 年度复盘卡 | P2 | M | v0.5 | 跨年时弹年度复盘卡：该年里程碑 + 进度对比 |
| F-07 | 生命阶段带 | P2 | M | v0.5 | 年表叠加求学/事业/退休等阶段色带 |
| F-10 | 导出为图片 | P2 | M | v0.5 | 当前人生表渲染成 PNG，纯本地 canvas，无后端（不称"分享"以免误读为社交）；含**全景周历**（80 行 × 52 列）导出格式——一屏放不下的周历属于海报（2026-08 脑暴共识） |
| F-01 | 打印 / 海报模式 | P2 | M | v0.5 | `@media print` 把人生表印成贴墙海报；浪漫但使用率未验证，降级至 v0.5 增量 |
| **—— 内容与体验（v0.6）——** | | | | | |
| B4+B5 | 内容国际化（名言 + 历史事件多语言） | P1 | M | v0.6 | **质量缺陷修复，非增强**：产品已上架五语言，但日/韩/英用户每天看到中文名言与中文历史事件；quotes.js 与 history.js 一并补齐 |
| A4 | 里程碑达成标记 | P2 | S | v0.6 | 里程碑可勾选"已完成"，达成态换色/换图形；未达成空色 → 达成亮色，强化人生表隐喻 |
| G1 | 统计单位切换（天/周/月） | P2 | S | v0.6 | "已度过 2,317 周"——《人生四千周》的文化共鸣；只动统计行不动网格签名，核心隐喻的最便宜深化 |
| H1 | 引导页"先看看效果" | P2 | S | v0.6 | 首次引导加演示模式（示例生日先看网格再提交）；情感型产品的激活摩擦就在"没看到价值先要数据"这一步 |
| B6 | 同龄人名人对照 | P1 | M | v0.6 | "34 岁的雷军即将创立小米"——按用户当前年龄匹配名人当年事件，统计行下方一行，每日轮换；**近现代名人**（科技/商业/文化/体育），不碰政治人物，考据须可查证；数据按语言分池，复用 B4+B5 内容架构 |
| B7 | 生日/新年特别呈现 | P2 | S | v0.6 | 生日当天今天格 accent 光晕增强 + 专属文案；1 月 1 日"这一年的第一格"；每年一次的安静仪式 |
| **—— 层级钻取（v0.7）——** | | | | | |
| V1 | 年→月→日层级钻取 | P1 | M | v0.7 | 嵌套规则「上=父级单位，下=子级网格」：点击年格后上方变为年份数字+12 月格，下方变为经典月历（周×日，周一起始）；本周整行 accent 描边；点月格切月份；点年份数字/Esc/再点返回。366 格年全景保留在层级 0，签名不动 |
| A5 | 里程碑模板包 | P2 | S | v0.7 | 添加里程碑时提供常见模板快填（家人生日/结婚纪念/宝宝出生/目标日），降低激活摩擦 |
| **—— 产品深化（v1.0）——** | | | | | |
| B1 | 时间胶囊 | P1 | M | v1.0 | 写一段话给未来的自己，设定解锁日期，到日子打开新标签页时展示。纯本地存储、零后端，不动日格交互；是 F-12 情感内核的轻量实现；**前置设计约束见 §7** |
| C1 | 人生账单视图 | P2 | M | v1.0 | 第二视图：已度过 43%；睡觉约用掉 26 年、工作 10 年、剩余自由时间 X 年。规则驱动本地计算，网格 ⇄ 账单切换，不破坏主界面 |
| **—— 文化差异化（v1.x，均反馈驱动）——** | | | | | |
| A2 | 农历里程碑 | P3 | L | 暂缓 | 农历生日/纪念日：内置 1900–2100 农历换算表（静态整数表，零依赖）；**暂缓，依用户诉求与反馈再评估**（≥3 条独立诉求），见 §6 |
| A3 | 节气标注 | P3 | S | 暂缓 | 24 节气在月格标注；**依赖 A2 数据表，随 A2 一并暂缓** |
| B3 | 季节氛围 | P3 | S | 暂缓 | 节气/节日当天微调 accent 或加问候；**依赖 A2 数据表，随 A2 一并暂缓** |
| G3 | 纪念里程碑（忌日/逝去的人） | P3 | M | v1.x | "已离开 N 天"型里程碑；扩展"一张表"到 remembrance，需反馈验证（≥3 条诉求），见 §7 |
| G4 | 你出生那年的世界 | P3 | S–M | v1.x | 用现有 history 数据，在出生年/生日格叠加"你来到这世界那天，世界在发生什么"；个人时间与宇宙时间的叙事交汇，可并入 F-08 |
| **—— 内容资产扩充（v1.x，也可作任意版本增量）——** | | | | | |
| E1 | 新预制主题（3–5 幅公有领域画作） | P2 | S/幅 | v1.x | 取色工具链现成（`tools/extract-colors.swift`），每幅成本极低，直接增强"每天几十次瞥见"的新鲜感——对最强资产最便宜的投资 |
| E2 | 用户自定义名言 | P3 | M | v1.x | 复用里程碑式 UI：用户加自己的句子进轮换池；纯本地，个性化深化 |
| E3 | 主题四季轮换 | P3 | S | v1.x | 主题选项加"四季"：3–5 月浪花 / 6–8 月阳伞 / 9–11 月麦田 / 12–2 月星夜，按月自动切换；opt-in，无需农历数据 |
| **—— 平台与分发（v1.x）——** | | | | | |
| D2 | 静态演示页 | P2 | S | v1.x | GitHub Pages 部署（localStorage 降级已支持），在线试玩转化 CWS 安装；纯静态零后端 |
| H3 | CWS 商店页五语言化 | P3 | S | v1.x | 商店描述/截图本地化，影响浏览→安装转化 |
| **—— 探索（需验证或权衡，v1.x）——** | | | | | |
| G2 | 倒数模式（剩余优先） | P3 | S | v1.x | 统计行改为"还剩 X 天"优先展示，opt-in；memento mori 的纯粹表达，但可能引发焦虑，默认关闭 |
| F-16 | 反馈入口 | P3 | S | v1.x | 设置内"反馈/评分"链接跳 CWS / mailto；不违反无追踪 |
| F-13 | 每周/里程碑提醒 | P3 | M | v1.x | `chrome.alarms` 推送；**需加权限**，先验证需求再权衡 |
| F-14 | 主题数据分享 | P3 | L | v1.x | 主题 JSON 纯数据导入导出或 share 链接，不经后端 |
| **—— 归属未来 Web 系统（插件内不做，见 §0.5）——** | | | | | |
| F-12 | 每日记录层 | — | XL | Web | XL 重交互 + 触碰日格只读红线 + 需要账号/同步支撑；归 Web 系统阶段单独立项，插件侧由 B1 覆盖核心情感需求 |
| F-15 | 多人生日历 | — | L | Web | 多人数据模型与设备同步属 Web 阶段能力 |
| **—— 明确不做（现阶段，可依诉求重评）——** | | | | | |
| — | 可调寿命（原 A1） | — | — | — | **80 年固定是刻意决策**：覆盖绝大部分人的有效生命、增强紧迫感，80+ 老年人非目标用户；若未来平均寿命大幅提升可评 96（十年尺度再议） |
| — | Edge / Firefox / Safari 移植（原 H2/D1） | — | — | — | **单一 Chrome 策略**，集中精力做市场验证；后续按用户诉求与数量再说 |
| — | 更多语言（法/德/西等） | — | — | — | 在 B4+B5 内容债务补齐之前，加语言只会扩大缺陷面 |
| — | streak / 成就 / 积分 | — | — | — | 违反游戏化红线（§0） |

---

## 4. 版本路线图

```
v0.3.0 [已发布]  基础稳固
   人生表 · 主题 · 里程碑 · i18n · 跨天刷新
        │
        ▼
v0.4.0 [下一步]  「便携与健壮」—— 修债 + 数据可携带
   TD-03 存储错误处理(P0)   TD-01/02/04 技术债(P1)
   F-02/03 数据导出导入      F-05 键盘导航   F-06 CSV 导出
   先修已知 bug 再加功能；不加新权限
        │
        ▼
v0.5.0  「观览延展」—— 在「看」的维度加层次与节奏
   F-09 倒计时   F-08 复盘卡   F-07 阶段带   F-10 导出图片
   F-01 打印海报   F-11 首日体验
        │
        ▼
v0.6.0  「内容与体验」—— 修内容缺陷 + 轻量体验项
   B4+B5 内容国际化(P1)   TD-05 最小测试(P1)   B6 同龄人名人(P1)
   A4 达成标记   G1 周单位   H1 演示模式   B7 生日/新年仪式
        │
        ▼
v0.7.0  「钻取」—— 年/月/周/日各就各位
   V1 层级钻取(P1)：上=父级单位，下=子级网格；本周行高亮
   A5 里程碑模板包
        │
        ▼
v1.0.0  「深化」—— 插件侧的成熟收尾：从「看表」到「与时间对话」
   B1 时间胶囊（M 级；承诺边界见 §7）
   C1 人生账单视图（洞察第二视图）
        │
        ▼
v1.x    「探索」—— 全部走反馈门槛（§6）
   G3 纪念里程碑   G4 出生那年的世界   E1 新主题   E2 自定义名言   E3 四季轮换
   D2 演示页   H3 商店页本地化   G2 倒数模式
   F-16 反馈入口  F-13 提醒(加权限)  F-14 主题分享
   A2 农历（暂缓）→ A3 节气 / B3 季节氛围 随 A2
        │
        ▼
——— 平台跃迁（市场验证成立后另立规划，见 §0.5）———
Web 系统：账号体系 · 云同步 · F-12 每日记录层 · F-15 多人生日历
App：用户粘性强则启动
```

**演进逻辑**：当前阶段的纲领是**低成本市场验证**（§0.5）——插件保持轻量，重功能不往插件里塞。v0.4 修债 + 数据可携带 → v0.5 观览维度加深 → v0.6 修掉内容国际化的质量缺陷（对现有非中文用户负责）+ 最小测试与轻量体验项（同龄人名人是内容线的第一根新枝）→ v0.7 用层级钻取把年/月/周/日四个时间单位各就各位（数量感全程可数，全景周历只作海报产物）→ v1.0 以时间胶囊完成插件侧「与时间对话」的成熟收尾 → v1.x 全部走反馈门槛，无诉求不动工。当插件验证成立（用户量与粘性达标），深度能力（记录层、多人、同步）在 Web 系统阶段另起炉灶，而不是让插件膨胀。

---

## 5. v0.4.0 详细设计方案

> 主题：**便携与健壮**。先修已知技术债（含一个 P0 数据安全项），再补齐数据可携带性。**不扩功能边界**（键盘导航与 CSV 是已有数据的出口与 a11y 补齐，非新能力）。

### 5.1 技术债修复（P0/P1，先于一切新功能）

#### TD-03 存储写入错误处理（P0）
- `saveRaw` / `saveBgImage` 捕获 `chrome.storage.set` 的 reject（`QUOTA_BYTES_PER_ITEM` / `QUOTA_BYTES` 超限）
- 抛出含中文原因的错误，调用方在设置面板顶部出红色提示：
  「保存失败：存储空间不足，请删除部分自定义主题或背景图」
- 落点：`src/lib/storage.js` 加 try/catch + 抛错；`src/settings-panel.js` 处理错误回调

#### TD-01 去除 settings-overlay 重复绑定（P1）
- 删除 `bindEvents()` 内 line 418–420 的 `settings-overlay` click 绑定
- 保留 `openSettingsModal()` 内带 `settingsMounted` 守卫的那一处（line 515）
- 这样遮罩点击关闭只在弹窗挂载后生效，且只绑一次

#### TD-02 IS_DEV 前置 + dev 参数精简（P1）
- 把 `const IS_DEV = ...` 从 line 60 移到模块顶部（import 之后、`init()` 调用之前），消除"定义前使用"的时序依赖
- 删除截图非必需的 `?year=` / `?lang=open`（后者与 storage.js 设置语言的 `?lang=` 同名冲突）；保留 `?today=` / `?settings=open` / 状态注入参数——它们是无头截图工作流的底座（2026-08 决策：保留机制、精简表面）

#### TD-04 i18n 缺键 dev 告警（P1）
- `t()` 缺 key 时 `console.warn('[i18n] missing key: ' + key)`（非扩展环境 / 开发时）
- 生产环境（扩展）静默返回 key 本身，不打扰用户

### 5.2 F-02 / F-03 数据导出与导入

**目标**：用户换设备、重装、备份时不丢一生数据。

**导出**（设置面板「导出数据」按钮）：
- 收集 `settings`（含 `milestones` / `customThemes`）+ 从 `chrome.storage.local` 读取所有 `bgimg-*` 背景图转 dataURL 内嵌
- 结构：
  ```json
  {
    "app": "life-calendar",
    "version": "0.4",
    "exportedAt": "2026-07-28T...",
    "settings": { ... },
    "bgImages": { "ct-demo2": "data:image/jpeg;base64,..." }
  }
  ```
- Blob 下载 `life-calendar-backup-YYYYMMDD.json`

**导入**（设置面板「导入数据」+ file input）：
- 读 JSON，校验 `app` / `version` / `settings` 结构完整性
- 策略：覆盖式写入 `settings`，背景图按 id 写入 `chrome.storage.local`
- 自定义主题 id 冲突时保留导入方
- 校验失败 → 友好错误提示（不复用 TD-03 的 quota 提示，区分"文件非法"与"空间不足"）；成功 → 刷新页面
- 安全：仅接受 JSON，限制大小（如 20MB），全程 try/catch

**落点**：`src/lib/storage.js` 加 `exportData()` / `importData(json)`；`src/settings-panel.js` 加两个控件与 i18n 键。

**复杂度 M**：两个存储函数 + 两个 UI 控件 + 5 语言 i18n + 边界处理。估时 3–4 天（含大图片 dataURL 边界、id 冲突、校验失败提示）。

### 5.3 F-05 键盘导航年格（P2）

- 年格加 `tabindex="0"`
- 网格容器监听 `keydown`：方向键 ←→↑↓ 移动选中年（5×16 矩阵换算行列），Enter 切换月表到该年，Esc 回到今年
- 选中年复用 `:focus-visible` 的 accent 焦点环
- 落点：`src/newtab.js` 加 keydown 监听 + tabindex，`src/newtab.css` 焦点样式已有

**复杂度 S**：0.5–1 天。

### 5.4 F-06 里程碑 CSV 导出（P2）

设置面板「导出 CSV」按钮，把 `settings.milestones` 序列化为 CSV（日期/标签/图标/重复），Blob 下载。**复杂度 S**：0.5 天。

### 5.5 验收标准

- [ ] TD-03：`chrome.storage` 写入超限时，UI 给出明确提示而非静默失败
- [ ] TD-01：遮罩点击关闭只触发一次（可用断点/计数验证）
- [ ] TD-02：`IS_DEV` 在模块顶部定义，`init()` 调用前已就绪（静态可读）；`?year=`/`?lang=open` 已移除，`?today=` 等截图参数仍生效
- [ ] TD-04：删一个 i18n key，开发预览时控制台见 warn，扩展环境用户侧无感知
- [ ] F-02/03：导出的 JSON 在另一空环境导入后，设置、自定义主题、背景图完整还原
- [ ] F-05：键盘能 Tab 到年格，方向键浏览，Enter 切换月表，Esc 回今年
- [ ] F-06：里程碑可导出 CSV 并在 Excel/Numbers 正确打开
- [ ] 14 个 JS 通过 `node --check`；打包 zip 结构与 v0.3.0 一致

### 5.6 工作拆分与估时

| 任务 | 复杂度 | 估时 |
| --- | --- | --- |
| TD-03 存储错误处理 | S | 0.5d |
| TD-01 去重复绑定 | S | 0.25d |
| TD-02 IS_DEV 前置 | S | 0.25d |
| TD-04 i18n 缺键告警 | S | 0.25d |
| F-02/F-03 导出导入 | M | 3–4d |
| F-05 键盘导航 | S | 0.5d |
| F-06 CSV 导出 | S | 0.5d |
| i18n 五语言 + 测试 + 截图验证 | — | 1.5d |
| **合计** | | **~7–8d** |

### 5.7 manifest 变更

- `version` → `"0.4.0"`
- **不加任何新权限**（全部在现有 `storage` 权限范围内）

---

## 6. 反馈验证机制（战略说明）

本项目红线明确**不做任何用户行为追踪/分析/埋点**。因此路线图中反复提到的"用反馈验证需求"只能靠以下定性来源：

1. **Chrome Web Store 评论** —— 主要信号源，被动收集
2. **邮件反馈**（隐私政策公示的邮箱）—— 主动用户
3. **GitHub Issues**（仓库公开）—— 技术用户

**触发门槛**：
- **B1 时间胶囊（v1.0 跃迁候选）**：v0.5/v0.6 期间若 CWS 评论、邮件或 Issues 出现 ≥3 条独立的"希望记录/写给未来的自己"类诉求 → 触发 B1 立项。门槛低于 F-12，因为它不触碰日格只读红线、成本小一个数量级。
- **A2 农历里程碑**：需 ≥3 条独立"农历生日/忌日"类诉求才立项；A3/B3 依赖其数据表，随 A2 一并评估。
- **B4+B5 内容国际化**：**不需要反馈门槛**——它是对现有非中文用户的质量缺陷修复，不是需求验证项。
- **G3 纪念里程碑**：需 ≥3 条独立"纪念/忌日"类诉求才立项。
- **观览/内容增强（V1/B6/B7/A5/E3 等）**：**不设反馈门槛**——门槛只用于定位跃迁、权限变更、文化假设类决策；此类增强由维护者按立项框架（§0 四条件）判断后直接排期。

F-16（反馈入口）在 v1.x 才做，正是因为当前反馈量不足以支撑其优先级——先有可验证的版本，再放大反馈漏斗。

---

## 7. kill criteria（放弃条件）

路线图不能只有向前走。以下情况应停止或回退对应 feature：

### B1 时间胶囊（v1.0 跃迁候选）
- **承诺与交付能力不匹配（前置设计约束）**：胶囊存 `storage.local` 意味着换设备即丢失，而用户写入时的心理预期是"它会在未来等我"。**立项前必须解决**：① 写入时明确告知"存于本机、换设备需用导出功能迁移"；② 时间跨度上限设为 5 年，不承诺更远的未来；③ 导出功能（F-02）必须把胶囊纳入备份。三条做不到则不立项
- **存储冲突**：长文胶囊超出 `storage.sync` 限额 → 必须走 `storage.local`（仅本机）；若用户换设备后胶囊"消失"引发困惑且告知无效 → 缩减为短句模式
- **错过解锁时机**：解锁依赖新标签页加载时检测，用户数日不开新标签页会错过解锁瞬间 → 可接受（它本就是"打开新标签页时遇见"的仪式），但若用户普遍反馈"根本没看到" → 增加未读角标提示
- **反馈不成立**：v0.5/v0.6 期间无 ≥3 条独立诉求 → 不立项，路线图停 v0.6

### C1 人生账单视图
- **喧宾夺主**：若账单视图被当作主界面而网格被弱化 → 回退，账单仅作第二视图，网格永远是签名

### V1 层级钻取
- **交互回归**：若自测/反馈显示"点击年格钻入"破坏既有"点年切下方年份"的习惯认知 → 回退为仅切下方（保留层级 0 行为），月历模式移至独立入口再评估
- **布局复杂度失控**：7 列月历与 32 列网格并存导致主题适配（5 预制 + 自定义）大面积破版 → 缩减为日格连续排布（不引入星期对齐），或停止

### B6 同龄人名人对照
- **考据质量不过关**：事件-年龄对照无法全部查证到可靠来源 → 宁可缩减条目数，不上讹传
- **政治敏感性**：政治人物一律不进入数据池（含正面评价）；i18n 分池时各地区池同样适用

### F-12 每日记录层
- **已重新归属（2026-08 决策）**：XL 重交互 + 触碰日格只读红线 + 需要账号/同步支撑 → 不在插件内实施，归未来 Web 系统阶段单独立项评估（§0.5）。插件侧由 B1 时间胶囊覆盖「与时间对话」的核心情感需求；Web 阶段的需求强度用 §6 同样的定性渠道评估。

### G3 纪念里程碑
- **情绪负担过重**：若"已离开 N 天"的倒数呈现对 grieving 用户造成持续刺痛而非抚慰 → 停止或改为仅正数纪念（"每年的这一天记得 TA"）
- **反馈不成立**：无 ≥3 条独立诉求 → 不立项

### E1 新预制主题
- **质量不过关**：新主题必须通过既有配色约定（从画作取色，`tools/extract-colors.swift`），若画作版权状态不明或取色效果达不到五套既有主题的水准 → 宁可不加

### H1 引导页演示模式
- **数据混淆**：演示数据必须不写入存储、退出演示后完全消失；若用户误把演示当真实数据保存 → 停止，回归纯引导

### F-13 提醒
- **权限论证不通过**：若 `chrome.alarms` 权限说明在 CWS 审核或用户认知中难以自洽 → 停止
- **无独立反馈需求**：未收到"希望提醒"类反馈 → 不做

### F-15 多人生日历
- **已重新归属（2026-08 决策）**：多人数据模型与设备同步属 Web 系统阶段能力，不在插件内实施（§0.5）。

### 通用
- 任何 feature 若实现中发现**必须引入后端或第三方服务才能成立** → 立即停止，该 feature 永久移出路线图（红线）
- 任何 feature 若实现中发现**必须让日格承载输入交互且交互冲突无解** → 立即停止或改为不动日格的替代方案（红线）
