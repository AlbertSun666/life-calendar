# 人生日历 · 需求与版本规划 ROADMAP

> 本文档记录已想到的功能点、优先级、复杂度评估与版本路线图，并给出当前迭代版本（v0.4）的设计方案。
> 维护原则：宁缺毋滥，每个候选都要回答「它是否深化了『把一生画成一张表』这一核心隐喻」。

---

## 0. 不可破的红线（贯穿所有版本）

- **零依赖、零构建、零后端** —— 原生 ES Modules，不引入框架/打包器/服务器
- **数据归用户** —— 一切数据存 `chrome.storage`（sync/local）或导出文件；任何云同步、账号体系、第三方服务一律不做
- **装饰必有主题语义** —— 耐看不花哨，第一万次打开依然安静得体
- **新权限必须可论证** —— 仅当功能无法在现有权限下实现时才申请，并在 release notes 说明用途

---

## 1. 已交付基线（v0.3.0，已上架 Chrome Web Store）

| 模块 | 内容 |
| --- | --- |
| 人生表 | 5×16 年格 + 当年 12 月逐日格；年/日格等宽对齐；过去灰/未来白/今天红；灰色每日自动推进 |
| 主题 | 5 预制主题（公有领域名画取色）+ 自定义主题编辑器 + 毛玻璃滑杆 + 主题栏快捷切换 |
| 里程碑 | 生日/纪念日/目标日，5 图标，支持每年重复或一次性，年格右上角标记点 |
| 内容 | 每日一句 + 历史上的今天，可独立开关，离线可用 |
| i18n | 简中/繁中/日/韩/英，主页语言按钮一键切换，默认跟随系统 |
| 体验 | 首次引导、跨天自动刷新、时区指定、设置弹窗与独立设置页 |

---

## 2. 等级定义

### 优先级（4 级）

| 级别 | 含义 | 决策 |
| --- | --- | --- |
| **P0 必做** | 核心体验必需 | 阻塞版本发布，无条件排期 |
| **P1 应做** | 显著提升价值或可靠性 | 本版本目标，尽力完成 |
| **P2 可做** | 增量价值，锦上添花 | 视用户反馈与排期择机 |
| **P3 暂缓** | 探索性或受硬约束权衡 | 需先验证需求或解决权衡，不承诺时间 |

### 复杂度（4 级）

| 级别 | 工作量 | 范围特征 |
| --- | --- | --- |
| **S 简单** | < 1 天 | 局部改动，单文件或单函数 |
| **M 中等** | 1–3 天 | 单模块或跨 2–3 文件，可能含新 UI 控件 |
| **L 复杂** | 3–7 天 | 跨多模块，需新数据模型 / 迁移 / 较大 UI |
| **XL 极复杂** | > 7 天 | 架构级或全新子系统，需独立设计阶段 |

---

## 3. 候选功能清单

| 编号 | 功能 | 优先级 | 复杂度 | 目标版本 | 说明 |
| --- | --- | --- | --- | --- | --- |
| F-01 | 打印 / 海报模式 | P1 | M | v0.4 | `@media print` 把人生表印成贴墙海报，兑现「画成一张表」 |
| F-02 | 数据导出 | P1 | M | v0.4 | JSON 导出全部设置+里程碑+自定义主题+背景图，可携带可备份 |
| F-03 | 数据导入 | P1 | M | v0.4 | JSON 导入并校验，换设备/重装无忧 |
| F-04 | 存储写入错误处理 | P1 | S | v0.4 | `saveSettings` 捕获 quota 超限，UI 明确提示而非静默失败 |
| F-05 | 键盘导航年格 | P2 | S | v0.4 | 年格方向键浏览 + Enter 切月表，补齐 a11y |
| F-06 | 里程碑 CSV 导出 | P2 | S | v0.4 | 里程碑清单导出 CSV，便于表格软件查看 |
| F-07 | 生命阶段带 | P2 | M | v0.5 | 年表叠加求学/事业/退休等阶段色带，复用 milestone 体系 |
| F-08 | 年度复盘卡 | P2 | M | v0.5 | 跨年时弹年度复盘卡：该年里程碑 + 进度对比 |
| F-09 | 纪念日倒计时 | P2 | M | v0.5 | 标题区显示距下一个里程碑的天数 |
| F-10 | 导出为图片分享 | P2 | M | v0.5 | 当前人生表渲染成 PNG，纯本地 canvas，无后端 |
| F-11 | 首日体验优化 | P2 | S | v0.5 | 引导卡预览主题、时区检测提示更友好 |
| F-12 | 每日记录层 | P2 | L | v1.0 | 给任意日格写一句话/心情，格子出小记号；从「观览」变「生活账本」 |
| F-13 | 每周/里程碑提醒 | P3 | M | v1.x | `chrome.alarms` 推送；**需加权限**，先验证需求再权衡 |
| F-14 | 主题数据分享 | P3 | L | v1.x | 主题 JSON 纯数据分享（导入导出或 share 链接，不经后端） |
| F-15 | 多人生日历 | P3 | L | v1.x | 家庭/伴侣/孩子多出生日期，多表叠加或切换 |
| F-16 | i18n 缺键告警 | P3 | S | v1.x | `t()` 缺键时 dev 控制台 warn，避免静默漏译 |

---

## 4. 版本路线图

```
v0.3.0 [已发布]  基础稳固
   人生表 · 主题 · 里程碑 · i18n · 跨天刷新
        │
        ▼
v0.4.0 [下一步]  「稳固」—— 让已有价值可携带、可分享、可信赖
   F-01 打印海报        F-02/03 数据导出导入
   F-04 存储错误处理     F-05 键盘导航      F-06 CSV 导出
   不扩功能边界，先把根扎深
        │
        ▼
v0.5.0  「延展」—— 在「看」的维度加层次与节奏
   F-07 生命阶段带   F-08 年度复盘卡   F-09 纪念日倒计时
   F-10 图片分享     F-11 首日体验优化
        │
        ▼
v1.0.0  「深化」—— 把「看」变成「写」（产品定位的关键跃迁）
   F-12 每日记录层（可选生活方式，先用 v0.4/v0.5 反馈验证）
        │
        ▼
v1.x    「探索」—— 需验证需求或解决权限权衡
   F-13 提醒(加权限)   F-14 主题分享   F-15 多人日历   F-16 i18n 健壮性
```

**演进逻辑**：先用 v0.4 把已有价值的「可携带性」与「可靠性」补齐（低风险高回报），再用 v0.5 在观览维度加深层次，最后用 v1.0 的每日记录层验证是否能从工具升级为生活账本——这一步若走通就是产品的灵魂跃迁，走不通也无损已稳固的基线。任何会击穿「零后端、数据归你」的方向（账号、云同步、社交后端、内置广告/分析）一律不列入路线图。

---

## 5. v0.4.0 详细设计方案

> 主题：**稳固**。不扩功能边界，让人生表的数据可携带、可打印、可信赖，并补齐基础可访问性。

### 5.1 F-01 打印 / 海报模式

**目标**：把人生表印成一张能贴墙上的海报，最字面地兑现「把一生画成一张表」。

**触发**：设置面板新增「打印海报」按钮（调用 `window.print()`）。

**实现**（集中在 `src/newtab.css`，零 JS 逻辑）：
```
@media print {
  @page { size: A4 landscape; margin: 12mm; }
  .header-actions, .settings-overlay, .onboarding, .life-progress { ... }
  .page { padding: 0; max-width: 100%; }
  /* 背景图关闭省墨，保留主题配色或降为黑白友好 */
  body::before, body::after { display: none; }
  /* 网格高对比：过去格加深、未来格留白、今天格保留强调 */
  /* 标题放大居中，附导出日期 */
}
```
- 打印时隐藏：设置/语言按钮、设置弹窗、引导层、进度条（可选保留）
- 背景：关闭画作背景图（省墨），保留主题配色或降阶为黑白友好
- 网格：过去格加深对比、未来格留白、今天格保留强调色
- 标题块放大居中，附生成日期角标
- 可选底部附里程碑清单（开关）

**复杂度 M**：以 CSS 为主 + 一个按钮控件 + `print()` 调用。

### 5.2 F-02 / F-03 数据导出与导入

**目标**：用户换设备、重装、备份时不丢一生数据。

**导出**（设置面板「导出数据」按钮）：
- 收集 `settings`（含 `milestones` / `customThemes`）+ 从 `chrome.storage.local` 读取所有 `bgimg-*` 背景图转 dataURL 内嵌
- 结构：
  ```json
  {
    "app": "life-calendar",
    "version": "0.4",
    "exportedAt": "2026-07-28T...",
    "settings": { ... },
    "bgImages": { "ct-demo2": "data:image/jpeg;base64,..." }
  }
  ```
- Blob 下载 `life-calendar-backup-YYYYMMDD.json`

**导入**（设置面板「导入数据」+ file input）：
- 读 JSON，校验 `app` / `version` / 结构完整性
- 策略：覆盖式写入 `settings`，背景图按 id 写入 `chrome.storage.local`
- 自定义主题 id 冲突时保留导入方
- 校验失败 → 友好错误提示；成功 → 刷新页面
- 安全：仅接受 JSON，限制大小，全程 try/catch

**落点**：`src/lib/storage.js` 加 `exportData()` / `importData(json)`，`src/settings-panel.js` 加两个控件与 i18n 键。

**复杂度 M**：两个存储函数 + 两个 UI 控件 + 5 语言 i18n。

### 5.3 F-04 存储写入错误处理

**目标**：`chrome.storage` 写入失败（quota 超限等）不再静默吞掉。

**实现**：
- `saveSettings` / `saveBgImage` 捕获 `chrome.storage.set` 的 reject（`QUOTA_BYTES_PER_ITEM` / `QUOTA_BYTES` 超限）
- 抛出/返回错误，调用方在设置面板顶部出红色提示：
  「保存失败：存储空间不足，请删除部分自定义主题或背景图」
- 落点：`src/lib/storage.js` 加 try/catch，`src/settings-panel.js` 处理错误回调

**复杂度 S**：局部改动，~0.5 天。

### 5.4 F-05 键盘导航年格

**目标**：年格支持键盘浏览，补齐代码 review 发现的 a11y 缺口。

**实现**：
- 年格加 `tabindex="0"`（或改为 `<button>`）
- 网格容器监听 `keydown`：方向键 ←→↑↓ 移动选中年（5×16 矩阵换算行列），Enter 切换月表到该年，Esc 回到今年
- 选中年复用 `:focus-visible` 的 accent 焦点环
- 落点：`src/newtab.js` 加 keydown 监听 + tabindex，`src/newtab.css` 焦点样式已有

**复杂度 S**：~0.5 天。

### 5.5 F-06 里程碑 CSV 导出

**目标**：里程碑清单可在表格软件中查看/整理。

**实现**：设置面板「导出 CSV」按钮，把 `settings.milestones` 序列化为 CSV（日期/标签/图标/重复），Blob 下载。**复杂度 S**。

### 5.6 验收标准

- [ ] 打印预览能输出一张干净的 A4 横向海报，网格清晰、无控件干扰
- [ ] 导出的 JSON 在另一空环境导入后，设置、自定义主题、背景图完整还原
- [ ] `chrome.storage` 写入超限时，UI 给出明确提示而非静默失败
- [ ] 键盘能 Tab 到年格，方向键浏览，Enter 切换月表
- [ ] 里程碑可导出 CSV 并在 Excel/Numbers 正确打开
- [ ] 14 个 JS 通过 `node --check`；打包 zip 结构与 v0.3.0 一致

### 5.7 工作拆分与估时

| 任务 | 复杂度 | 估时 |
| --- | --- | --- |
| F-04 存储错误处理 | S | 0.5d |
| F-05 键盘导航 | S | 0.5d |
| F-06 CSV 导出 | S | 0.5d |
| F-01 打印海报 | M | 1.5d |
| F-02/F-03 导出导入 | M | 2d |
| i18n 五语言 + 测试 + 截图验证 | — | 1d |
| **合计** | | **~6d** |

### 5.8 manifest 变更

- `version` → `"0.4.0"`
- **不加任何新权限**（F-01~F-06 全在现有 `storage` 权限范围内）
