# 人生日历 · 需求与版本规划 ROADMAP

> 本文档记录已想到的功能点、优先级、复杂度评估与版本路线图，并给出当前迭代版本（v0.4）的设计方案。
> 维护原则：宁缺毋滥，每个候选都要回答「它是否深化了『把一生画成一张表』这一核心隐喻」。

---

## 0. 不可破的红线（贯穿所有版本）

- **零依赖、零构建、零后端** —— 原生 ES Modules，不引入框架/打包器/服务器
- **数据归用户** —— 一切数据存 `chrome.storage`（sync/local）或导出文件；任何云同步、账号体系、第三方服务一律不做
- **装饰必有主题语义** —— 耐看不花哨，第一万次打开依然安静得体
- **新权限必须可论证** —— 仅当功能无法在现有权限下实现时才申请，并在 release notes 说明用途
- **不做用户行为追踪/分析** —— 因此"验证需求"只能靠 CWS 评论、邮件等定性反馈，见 §6
- **日格只读（美学红线）** —— 日格是「时间流逝」的静态呈现，是设计签名元素；任何需要日格承载输入交互的功能（如 F-12）必须先论证交互冲突，论证不过则保持只读
- **签名纯净** —— 不引入天气/待办/时钟等与「人生表」无关的 widget，不稀释核心隐喻

---

## 1. 已交付基线（v0.3.0，已上架 Chrome Web Store）

| 模块 | 内容 |
| --- | --- |
| 人生表 | 5×16 年格 + 当年 12 月逐日格；年/日格等宽对齐；过去灰/未来白/今天红；灰色每日自动推进 |
| 主题 | 5 预制主题（公有领域名画取色）+ 自定义主题编辑器 + 毛玻璃滑杆 + 主题栏快捷切换 |
| 里程碑 | 生日/纪念日/目标日，5 图标，支持每年重复或一次性，年格右上角标记点 |
| 内容 | 每日一句 + 历史上的今天，可独立开关，离线可用 |
| i18n | 简中/繁中/日/韩/英，主页语言按钮一键切换，默认跟随系统 |
| 体验 | 首次引导、跨天自动刷新、时区指定、设置弹窗与独立设置页 |

### 1.1 已知技术债（来自 v0.3 代码 review，未修）

| 编号 | 问题 | 现状 | 危险度 |
| --- | --- | --- | --- |
| TD-01 | `settings-overlay` click 绑定重复 | `bindEvents()` 与 `openSettingsModal()` 各绑一次遮罩点击关闭；因 `closeSettingsModal` 幂等当前无可见 bug，但是冗余代码 + 隐患 | 低（当前）/ 中（若关闭逻辑变非幂等） |
| TD-02 | `IS_DEV` 在定义前被使用 | `init()` 内 line 47 用 `IS_DEV`，`const IS_DEV` 在 line 60 声明；依赖 async 时序碰巧不报错，属脆弱写法 | 低（当前）/ 中（重构时易踩雷） |
| TD-03 | 存储写入无错误处理 | `saveRaw`/`saveBgImage` 不捕获 `chrome.storage` quota 超限，静默失败 | 中（用户数据丢失风险） |
| TD-04 | i18n `t()` 缺键静默 | 缺 key 返回 key 本身，无 dev 告警，漏译不易发现 | 低 |

---

## 2. 等级定义

### 优先级（4 级）

| 级别 | 含义 | 决策 |
| --- | --- | --- |
| **P0 必做** | 核心体验必需 / 已知 bug 影响数据安全 | 阻塞版本发布，无条件排期 |
| **P1 应做** | 显著提升价值或可靠性 | 本版本目标，尽力完成 |
| **P2 可做** | 增量价值，锦上添花 | 视用户反馈与排期择机 |
| **P3 暂缓** | 探索性或受硬约束权衡 | 需先验证需求或解决权衡，不承诺时间 |

### 复杂度（4 级）

| 级别 | 工作量 | 范围特征 |
| --- | --- | --- |
| **S 简单** | 0.5–1 天 | 局部改动，单文件或单函数 |
| **M 中等** | 2–4 天 | 单模块或跨 2–3 文件，可能含新 UI 控件 |
| **L 复杂** | 5–10 天 | 跨多模块，需新数据模型 / 迁移 / 较大 UI |
| **XL 极复杂** | > 10 天 | 架构级或全新子系统，需独立设计阶段 |

> 复杂度评估含 i18n 五语言、测试、截图验证，不是单纯写代码时间。

---

## 3. 候选功能清单

> 编号约定：TD = 技术债；F = 原功能清单（沿用）；A = 时间观（可调寿命/农历）；B = 仪式感（胶囊/名言）；C = 数据洞察（人生账单）；D = 分发与增长（Firefox/演示页）。

| 编号 | 功能 | 优先级 | 复杂度 | 目标版本 | 说明 |
| --- | --- | --- | --- | --- | --- |
| **—— 技术债修复 ——** | | | | | |
| TD-03 | 存储写入错误处理 | P0 | S | v0.4 | 见 §1.1；数据安全相关，优先于新功能 |
| TD-01 | 去除 settings-overlay 重复绑定 | P1 | S | v0.4 | 删 `bindEvents` 内那行，保留 `openSettingsModal` 内带守卫的绑定 |
| TD-02 | IS_DEV 定义前置 | P1 | S | v0.4 | 移到模块顶部，消除时序依赖 |
| TD-04 | i18n 缺键 dev 告警 | P1 | S | v0.4 | `t()` 缺键时 `console.warn`，顺手做掉 |
| **—— 数据可携带（v0.4）——** | | | | | |
| F-02 | 数据导出 | P1 | M | v0.4 | JSON 导出全部设置+里程碑+自定义主题+背景图 |
| F-03 | 数据导入 | P1 | M | v0.4 | JSON 导入并校验，换设备/重装无忧 |
| F-06 | 里程碑 CSV 导出 | P2 | S | v0.4 | 里程碑清单导出 CSV，便于表格软件查看 |
| **—— 可访问性 ——** | | | | | |
| F-05 | 键盘导航年格 | P2 | S | v0.4 | 方向键浏览 + Enter 切月表，补齐 a11y |
| F-11 | 首日体验优化 | P2 | S | v0.5 | 引导卡预览主题、时区检测提示更友好 |
| **—— 观览延展（v0.5）——** | | | | | |
| F-09 | 纪念日倒计时 | P2 | M | v0.5 | 标题区显示距下一个里程碑的天数 |
| F-08 | 年度复盘卡 | P2 | M | v0.5 | 跨年时弹年度复盘卡：该年里程碑 + 进度对比 |
| F-07 | 生命阶段带 | P2 | M | v0.5 | 年表叠加求学/事业/退休等阶段色带 |
| F-10 | 导出为图片 | P2 | M | v0.5 | 当前人生表渲染成 PNG，纯本地 canvas，无后端（不称"分享"以免误读为社交） |
| F-01 | 打印 / 海报模式 | P2 | M | v0.5 | `@media print` 把人生表印成贴墙海报；浪漫但使用率未验证，降级至 v0.5 增量 |
| **—— 时间观与体验（v0.5）——** | | | | | |
| A1 | 可调寿命 | P1 | M | v0.5 | 寿命 70/80/90/100 四档可选，年格总数与布局自适应；80 年是写死的产品假设，长度应由用户决定 |
| A4 | 里程碑达成标记 | P2 | S | v0.5 | 里程碑可勾选"已完成"，达成态换色/换图形；未达成空色 → 达成亮色，强化人生表隐喻 |
| B4 | 名言库多语言补齐 | P2 | S | v0.5 | 当前名言库以中文为主（95 句），其他语言用户看到的是中文每日一句——先补齐 5 语言内容再谈新功能 |
| **—— 产品深化（v1.0）——** | | | | | |
| B1 | 时间胶囊 | P1 | M | v1.0 | 写一段话给未来的自己，设定解锁日期（下一个生日/5 年后/退休日），到日子打开新标签页时展示。纯本地存储、零后端，不动日格交互；是 F-12 情感内核的轻量实现，见 §7 |
| C1 | 人生账单视图 | P2 | M | v1.0 | 第二视图：已度过 43%；睡觉约用掉 26 年、工作 10 年、剩余自由时间 X 年。规则驱动本地计算，网格 ⇄ 账单切换，不破坏主界面 |
| F-12 | 每日记录层 | P2 | XL | v1.0 | 给任意日格写一句话/心情，格子出小记号；从「观览」变「生活账本」；跨多模块新子系统，且触碰日格只读红线；见 §7 kill criteria，B1 落地后重估 |
| **—— 时间观·文化差异化（v1.x）——** | | | | | |
| A2 | 农历里程碑 | P1 | L | v1.x | 农历生日/纪念日：内置 1900–2100 农历换算表（静态整数表，零依赖），里程碑支持农历日期；中文市场的差异化武器 |
| A3 | 节气标注 | P2 | S | v1.x | 24 节气在月格标注，复用 A2 数据表；把中国文化的时间感带进日历 |
| B3 | 季节氛围 | P3 | S | v1.x | 节气/节日当天微调 accent 或加一句问候，让每次瞥见有变化；复用 A2 数据 |
| **—— 分发与增长（v1.x）——** | | | | | |
| D1 | Firefox 移植 | P3 | L | v1.x | MV3 API 几乎相同（`browser.*` 适配层已有基础）；Firefox 用户隐私敏感、与产品定位高度重合，是增量最大的分发渠道 |
| D2 | 静态演示页 | P2 | S | v1.x | GitHub Pages 部署（localStorage 降级已支持），在线试玩转化 CWS 安装；纯静态零后端 |
| **—— 探索（需验证或权衡，v1.x）——** | | | | | |
| F-16 | 反馈入口 | P3 | S | v1.x | 设置内"反馈/评分"链接跳 CWS / mailto；不违反无追踪 |
| F-13 | 每周/里程碑提醒 | P3 | M | v1.x | `chrome.alarms` 推送；**需加权限**，先验证需求再权衡 |
| F-14 | 主题数据分享 | P3 | L | v1.x | 主题 JSON 纯数据导入导出或 share 链接，不经后端 |
| F-15 | 多人生日历 | P3 | L | v1.x | 家庭/伴侣/孩子多出生日期，多表叠加或切换 |

---

## 4. 版本路线图

```
v0.3.0 [已发布]  基础稳固
   人生表 · 主题 · 里程碑 · i18n · 跨天刷新
        │
        ▼
v0.4.0 [下一步]  「便携与健壮」—— 修债 + 数据可携带
   TD-03 存储错误处理(P0)   TD-01/02/04 技术债(P1)
   F-02/03 数据导出导入      F-05 键盘导航   F-06 CSV 导出
   先修已知 bug 再加功能；不加新权限
        │
        ▼
v0.5.0  「延展」—— 在「看」的维度加层次与节奏
   第一批 · 观览核心：F-09 倒计时  F-08 复盘卡  F-07 阶段带  F-10 导出图片
   第二批 · 时间观与体验：A1 可调寿命  A4 里程碑达成  B4 名言多语言
                       F-01 打印海报  F-11 首日体验
   （体量较大，实际按两批先后交付）
        │
        ▼
v1.0.0  「深化」—— 从「看表」到「与时间对话」（定位的关键跃迁）
   B1 时间胶囊（跃迁候选，M 级，先于 F-12）
   C1 人生账单视图（洞察第二视图）
   F-12 每日记录层：受 kill criteria 约束，B1 落地后重估，否则跳过
        │
        ▼
v1.x    「探索与分发」—— 需验证需求或解决权限/平台权衡
   A2 农历里程碑（差异化武器，v1.x 最高优先）
   A3 节气标注   B3 季节氛围   D2 静态演示页
   F-16 反馈入口  F-13 提醒(加权限)  F-14 主题分享  F-15 多人日历
   D1 Firefox 移植
```

**演进逻辑**：v0.4 先把已知技术债和数据可携带性补齐（低风险、对用户负责），再在 v0.5 的观览维度加深层次，并补上三个不扩界面边界的「时间观」S/M 项（可调寿命、达成标记、名言多语言）。v1.0 的跃迁候选从「每日记录层」调整为「时间胶囊」——同样触及『与时间对话』的情感内核，成本却小一个数量级（M vs XL），且完全不触碰日格交互的美学红线；F-12 降为条件立项，B1 落地后根据用户行为重估。v1.x 把中文差异化（农历）与分发（Firefox/演示页）纳入视野——走不通也无损已稳固的基线。

---

## 5. v0.4.0 详细设计方案

> 主题：**便携与健壮**。先修已知技术债（含一个 P0 数据安全项），再补齐数据可携带性。**不扩功能边界**（键盘导航与 CSV 是已有数据的出口与 a11y 补齐，非新能力）。

### 5.1 技术债修复（P0/P1，先于一切新功能）

#### TD-03 存储写入错误处理（P0）
- `saveRaw` / `saveBgImage` 捕获 `chrome.storage.set` 的 reject（`QUOTA_BYTES_PER_ITEM` / `QUOTA_BYTES` 超限）
- 抛出含中文原因的错误，调用方在设置面板顶部出红色提示：
  「保存失败：存储空间不足，请删除部分自定义主题或背景图」
- 落点：`src/lib/storage.js` 加 try/catch + 抛错；`src/settings-panel.js` 处理错误回调

#### TD-01 去除 settings-overlay 重复绑定（P1）
- 删除 `bindEvents()` 内 line 418–420 的 `settings-overlay` click 绑定
- 保留 `openSettingsModal()` 内带 `settingsMounted` 守卫的那一处（line 515）
- 这样遮罩点击关闭只在弹窗挂载后生效，且只绑一次

#### TD-02 IS_DEV 定义前置（P1）
- 把 `const IS_DEV = ...` 从 line 60 移到模块顶部（import 之后、`init()` 调用之前）
- 消除"定义前使用"的时序依赖，让代码静态可读

#### TD-04 i18n 缺键 dev 告警（P1）
- `t()` 缺 key 时 `console.warn('[i18n] missing key: ' + key)`（非扩展环境 / 开发时）
- 生产环境（扩展）静默返回 key 本身，不打扰用户

### 5.2 F-02 / F-03 数据导出与导入

**目标**：用户换设备、重装、备份时不丢一生数据。

**导出**（设置面板「导出数据」按钮）：
- 收集 `settings`（含 `milestones` / `customThemes`）+ 从 `chrome.storage.local` 读取所有 `bgimg-*` 背景图转 dataURL 内嵌
- 结构：
  ```json
  {
    "app": "life-calendar",
    "version": "0.4",
    "exportedAt": "2026-07-28T...",
    "settings": { ... },
    "bgImages": { "ct-demo2": "data:image/jpeg;base64,..." }
  }
  ```
- Blob 下载 `life-calendar-backup-YYYYMMDD.json`

**导入**（设置面板「导入数据」+ file input）：
- 读 JSON，校验 `app` / `version` / `settings` 结构完整性
- 策略：覆盖式写入 `settings`，背景图按 id 写入 `chrome.storage.local`
- 自定义主题 id 冲突时保留导入方
- 校验失败 → 友好错误提示（不复用 TD-03 的 quota 提示，区分"文件非法"与"空间不足"）；成功 → 刷新页面
- 安全：仅接受 JSON，限制大小（如 20MB），全程 try/catch

**落点**：`src/lib/storage.js` 加 `exportData()` / `importData(json)`；`src/settings-panel.js` 加两个控件与 i18n 键。

**复杂度 M**：两个存储函数 + 两个 UI 控件 + 5 语言 i18n + 边界处理。估时 3–4 天（含大图片 dataURL 边界、id 冲突、校验失败提示）。

### 5.3 F-05 键盘导航年格（P2）

- 年格加 `tabindex="0"`
- 网格容器监听 `keydown`：方向键 ←→↑↓ 移动选中年（5×16 矩阵换算行列），Enter 切换月表到该年，Esc 回到今年
- 选中年复用 `:focus-visible` 的 accent 焦点环
- 落点：`src/newtab.js` 加 keydown 监听 + tabindex，`src/newtab.css` 焦点样式已有

**复杂度 S**：0.5–1 天。

### 5.4 F-06 里程碑 CSV 导出（P2）

设置面板「导出 CSV」按钮，把 `settings.milestones` 序列化为 CSV（日期/标签/图标/重复），Blob 下载。**复杂度 S**：0.5 天。

### 5.5 验收标准

- [ ] TD-03：`chrome.storage` 写入超限时，UI 给出明确提示而非静默失败
- [ ] TD-01：遮罩点击关闭只触发一次（可用断点/计数验证）
- [ ] TD-02：`IS_DEV` 在模块顶部定义，`init()` 调用前已就绪（静态可读）
- [ ] TD-04：删一个 i18n key，开发预览时控制台见 warn，扩展环境用户侧无感知
- [ ] F-02/03：导出的 JSON 在另一空环境导入后，设置、自定义主题、背景图完整还原
- [ ] F-05：键盘能 Tab 到年格，方向键浏览，Enter 切换月表，Esc 回今年
- [ ] F-06：里程碑可导出 CSV 并在 Excel/Numbers 正确打开
- [ ] 14 个 JS 通过 `node --check`；打包 zip 结构与 v0.3.0 一致

### 5.6 工作拆分与估时

| 任务 | 复杂度 | 估时 |
| --- | --- | --- |
| TD-03 存储错误处理 | S | 0.5d |
| TD-01 去重复绑定 | S | 0.25d |
| TD-02 IS_DEV 前置 | S | 0.25d |
| TD-04 i18n 缺键告警 | S | 0.25d |
| F-02/F-03 导出导入 | M | 3–4d |
| F-05 键盘导航 | S | 0.5d |
| F-06 CSV 导出 | S | 0.5d |
| i18n 五语言 + 测试 + 截图验证 | — | 1.5d |
| **合计** | | **~7–8d** |

### 5.7 manifest 变更

- `version` → `"0.4.0"`
- **不加任何新权限**（全部在现有 `storage` 权限范围内）

---

## 6. 反馈验证机制（战略说明）

本项目红线明确**不做任何用户行为追踪/分析/埋点**。因此路线图中反复提到的"用反馈验证需求"只能靠以下定性来源：

1. **Chrome Web Store 评论** —— 主要信号源，被动收集
2. **邮件反馈**（隐私政策公示的邮箱）—— 主动用户
3. **GitHub Issues**（仓库公开）—— 技术用户

**触发门槛**：
- **B1 时间胶囊（v1.0 跃迁候选）**：v0.5 期间若 CWS 评论、邮件或 Issues 出现 ≥3 条独立的"希望记录/写给未来的自己"类诉求 → 触发 B1 立项。门槛低于 F-12，因为它不触碰日格只读红线、成本小一个数量级。
- **F-12 每日记录层**：需要 ≥3 条独立"希望能记录每天"类诉求，**且** B1 落地后证明"日格可写"的交互冲突有解，才重新评估立项；否则 v1.0 不强推，路线图停留在"时间胶囊 + 人生账单"。

F-16（反馈入口）在 v1.x 才做，正是因为当前反馈量不足以支撑其优先级——先有可验证的版本，再放大反馈漏斗。

---

## 7. kill criteria（放弃条件）

路线图不能只有向前走。以下情况应停止或回退对应 feature：

### B1 时间胶囊（v1.0 跃迁候选）
- **存储冲突**：长文胶囊超出 `storage.sync` 限额 → 必须走 `storage.local`（仅本机）；若用户换设备后胶囊"消失"引发困惑 → 在设定时明确"仅存本机"说明；若反馈仍集中于此 → 缩减为短句模式
- **错过解锁时机**：解锁依赖新标签页加载时检测，用户数日不开新标签页会错过解锁瞬间 → 可接受（它本就是"打开新标签页时遇见"的仪式），但若用户普遍反馈"根本没看到" → 增加未读角标提示
- **反馈不成立**：v0.5 期间无 ≥3 条独立诉求 → 不立项，路线图停 v0.5 的观览延展

### A1 可调寿命
- **布局破坏**：任意寿命值会破坏 5×16 签名网格 → 只提供 70/80/90/100 四档且均能组成整数矩阵；若四档也无法满足设计约束 → 停止，维持 80 年

### C1 人生账单视图
- **喧宾夺主**：若账单视图被当作主界面而网格被弱化 → 回退，账单仅作第二视图，网格永远是签名

### F-12 每日记录层
- **交互冲突无法调和**：日格点击已有"切月表"职责，若叠加"写记录"导致两种意图打架、且无优雅方案 → 停止，保留纯观览定位（B1 已覆盖大部分情感需求时同样停止）
- **存储压力超限**：80 年 × 365 天 × 100 字节 ≈ 3MB 理论可行，但若实际用户长文记录使 `chrome.storage.local` 频繁触顶 → 缩减范围或停止
- **反馈不成立**：v0.4/v0.5 期间无 ≥3 条独立定性诉求 → 不立项，路线图停 v0.5
- **体验劣化**：记号密度过高破坏"安静得体"的界面气质 → 回退至纯观览

### F-13 提醒
- **权限论证不通过**：若 `chrome.alarms` 权限说明在 CWS 审核或用户认知中难以自洽 → 停止
- **无独立反馈需求**：未收到"希望提醒"类反馈 → 不做

### F-15 多人生日历
- **界面复杂度失控**：多表叠加破坏"一屏一焦点"原则，且切换方案不够直觉 → 停止
- **数据模型冲突**：多人里程碑/主题体系交织导致存储结构剧增 → 停止

### 通用
- 任何 feature 若实现中发现**必须引入后端或第三方服务才能成立** → 立即停止，该 feature 永久移出路线图（红线）
- 任何 feature 若实现中发现**必须让日格承载输入交互且交互冲突无解** → 立即停止或改为不动日格的替代方案（红线）
